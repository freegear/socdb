rtc-app.c :
	RTC test application C source


Makefile :
	Makefile


rtc-app :
	RTC test application image


Copy rtc-app.c and Makefile to temporary directory on Linux PC
and then, 

> make

finally, you will see "rtc-app" image file
(arm toolchain must be installed before "make")